<div class="row">
  <div class="col-md-6 col-md-offset-3 text-center">
    <% if params[:plan] == '3' %>
      <h1>Pro Account</h1>
      <p>Sign up for free to the community.</p>
    <% else %>
      <h1>Basic Account</h1>
      <p>Sign up for the Pro account at 10€/month to see everyones contacts.</p>
    <% end %>
  </div>
  <div class="col-md-6 col-md-offset-3">
    <div class="well">
      <h2>Sign up</h2>
      
      <% if params[:plan] == '3' %>
      <%= render "pro_form" %>
      <% else %>
      <%= render "basic_form" %>
      <% end %>
      
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>